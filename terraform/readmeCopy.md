# Python Module
1. This is the S2S translation service in Python.
2. Make sure you create a virtual env in your local for testing locally.
3. Install the requirements.txt using pip install -r requirements.txt in the activated virtual env.

# Electron App
1. Install the packages from package.json in the root directory.
2. Run "npm run start" in the virtual env